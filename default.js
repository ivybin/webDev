function addform(){
    const pesq=document.getElementById("formulario");
    if (pesq.classList) pesq.classList.add("epi");
    else pesq.classList += "epi";
    console.log(formulario);
    
}
function confirm(){
    const nome=document.getElementById("nome");
    console.log(nome);
}